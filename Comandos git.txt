Aula 01 - Comandos Git
git init - inicia o repositório local
git status - verifica mudanças no repositório local
git add nome do arquivo que houve a mudança
git commit -m "mensagem do commit"
git log - histórico de alterações no repositório
git config --global user.name "nome usuario do git"
git config --global user.email "email do git"